{
  "name": "url-shortener",
  "version": "1.0.0",
  "private": true,
  "description": "URL shortener monorepo with pnpm workspaces",
  "license": "UNLICENSE",
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "dev": "pnpm --parallel --filter \"./apps/*\" dev",
    "build": "pnpm -r run build"
  }
}
